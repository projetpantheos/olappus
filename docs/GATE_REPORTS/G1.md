# RAPPORT DE GATE — G1 Foundation

Date : 2026-09-08 · Format imposé par `RUN-26_FOUNDER_CLAUDE_RUNBOOK`

---

## GATE

**G1 — Foundation.** Condition de sortie (`RUN-42`) : dépôt initialisé, `.gitignore`, secret scan vert, TypeScript strict, Expo, CI, séparation d'environnements, version de Node épinglée.

## STATUS

**PASSED**, sous réserve de revue fondateur. Aucun point rouge.

## EVIDENCE

| Exigence                                  | Preuve                                                                                                                                                                                         |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dépôt initialisé                          | 3 commits sur `main`, aucun push                                                                                                                                                               |
| `.gitignore` avant tout `git add`         | premier fichier écrit du dépôt, avant `git init`-add                                                                                                                                           |
| Scan de secrets vert                      | `tooling/secret-scan.mjs` — 145 fichiers analysés, 0 détection, exécuté avant chaque commit                                                                                                    |
| TypeScript strict                         | TS 6.0.3, `strict` + `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`, `noImplicitOverride`, `noImplicitReturns` — appliqué aux paquets **et** à l'application |
| Expo                                      | SDK 57, React Native 0.86.3, expo-router 57, React 19.2.3                                                                                                                                      |
| **L'application se construit réellement** | `expo export --platform web` : 776 modules, 3 routes statiques générées                                                                                                                        |
| CI                                        | `.github/workflows/ci.yml` — secret scan en premier, puis typecheck, lint, format, tests, audit                                                                                                |
| Séparation d'environnements               | `.env.example` documenté, `OLAPPUS_ENV`, secrets serveur explicitement séparés du bundle                                                                                                       |
| Node épinglé                              | `.nvmrc` = 24, `engines` = `>=24 <25`, même version en CI                                                                                                                                      |
| Chaîne complète                           | `npm run ci` — **EXIT 0**                                                                                                                                                                      |

Commits :

```
69def6f  feat(mobile): application Expo minimale (G1)
1597965  build: socle monorepo, TypeScript strict et CI (G1)
d8fa3f8  chore: socle documentaire Olappus (post-G0)
```

## RISKS

1. **14 vulnérabilités modérées** transitives dans la chaîne Expo : `decode-uri-component` via `expo-router` (déni de service sur URL malformée — surface réelle : les liens profonds) et `uuid <11.1.1` via `@expo/config-plugins` (outillage de build). Aucune haute ni critique ; le seuil CI (`--audit-level=high`) reste donc significatif. **`npm audit fix --force` redescendrait `expo` en 46 et `expo-router` en 5** : non appliqué. À réexaminer à chaque montée de SDK.
2. **Couverture de tests symbolique** : 2 tests unitaires. La chaîne existe, la substance viendra avec le Core (G3) et les fixtures (G4).
3. **Aucun test de l'application mobile** : ni rendu, ni navigation. Le choix d'outillage pour les tests de composants React Native (`jest-expo`) n'est pas fait — vitest couvre les paquets, pas l'UI. À trancher avant G4.
4. **Le lint d'architecture n'existe pas encore** : l'invariant « aucune dépendance module → module » (`SEC-23`) n'est pas encore vérifié automatiquement. Prévu en G3, quand il y aura des modules.

## OPEN DECISIONS

Aucune décision RED ouverte.

Deux points mineurs tranchés en autonomie pendant la gate, signalés ici pour information :

- **TypeScript 6.0.3 et non 7.0.2** : `typescript-eslint` 8.70 déclare la plage supportée `>=4.8.4 <6.1.0`. Passer à TS 7 aurait cassé la chaîne de lint. Le template Expo SDK 57 épingle d'ailleurs la même version. À rouvrir quand `typescript-eslint` supportera la 7.
- **Retrait de la démo du template Expo** : ses onglets « Home / Explore » contredisent la navigation à quatre entrées de `PRD-14`. La `LICENSE` MIT d'Expo a également été retirée : elle aurait faussement suggéré une licence permissive sur un dépôt privé `UNLICENSED`.

## ROLLBACK

- Chaque commit est autonome et réversible ; aucun push n'a été fait, donc aucune coordination externe n'est nécessaire.
- Aucune migration, aucun schéma de base, aucune donnée : rien à défaire côté persistance.
- Un instantané du kit d'avant l'étape 0 existe hors dépôt.
- Revenir à l'état documentaire pur : `git reset --hard d8fa3f8`.

## NEXT GATE

**G2 — Privacy / Security baseline.**

Chemin critique : `governance/data_registry.yaml`. C'est lui qui doit générer les politiques RLS, les jobs de rétention, le paquet d'export, le plan de suppression et les contrôles du AI Gateway. Tant qu'il n'existe pas, la gouvernance des données reste déclarative.

Ensuite : quarantine, normalisation, identité / appareil / permissions, RLS par schéma (ADR-0005), et surtout la **preuve automatisée qu'un accès non autorisé échoue** — test d'acceptation n°13, contrôle n°1 de la matrice de sécurité.

Prérequis déjà satisfaits : Docker et WSL2 opérationnels (ADR-0009).

## CE QUE LE FONDATEUR DOIT JUGER

`RUN-26` prévient : ne pas juger une gate au seul fait qu'elle marche.

- **Valeur produit** : nulle à ce stade, et c'est normal. G1 ne produit rien de visible.
- **Confidentialité** : aucune donnée réelle, aucun compte réel, aucun secret dans le dépôt.
- **Sécurité** : le scan de secrets tourne avant chaque commit, pas seulement en CI.
- **Provenance** : chaque décision d'outillage est justifiée par une contrainte vérifiable, pas par une préférence.
- **Exploitabilité** : `npm run ci` reproduit localement exactement ce que fait la CI.
- **Rollback** : trivial, aucun état externe.
- **Charge cognitive** : l'application affiche trois lignes. Rien n'a été ajouté « pour plus tard ».
