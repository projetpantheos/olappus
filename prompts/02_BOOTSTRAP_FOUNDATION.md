# Prompt 02 — Bootstrap
Only after G0 approval. Create the monorepo, strict TypeScript baseline, Expo app, Supabase integration scaffold, CI, test harness, linting, formatting, environment separation and safe configuration. No real accounts. No production secrets. Run all baseline checks.
