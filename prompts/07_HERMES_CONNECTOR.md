# Prompt 07 — Hermès
Implement Gmail connector in a quarantined pipeline. Request minimum scopes, normalize extracted fields, minimize before any AI call, create Cases only after Core validation, and keep raw retention bounded.
