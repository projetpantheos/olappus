# Prompt 11 — New Module

Before creating any new module, fill the Module Manifest, define data fields, permissions, commands, queries, events, knowledge/rules, AI policy, risk, retention, external actions and acceptance tests. Then implement through Core contracts only.
